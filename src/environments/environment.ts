export const environment = {
  // 🛡️ USER ACCESS KEY (Judges/Public)
  honey_pot: 'sentinel-master-key',

  // 🕵️ INTERNAL MASTER KEY (For High-Level Direct Engagement)
  _internal_sk: '',

  // 📡 BACKEND SERVICE URL 
  // Use relative URL so it works both locally AND when served from backend
  apiUrl: ''
};
